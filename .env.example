# Copie este arquivo para ".env.local" para conectar o frontend à API real (pasta server/).
# Sem essa variável definida, o app funciona normalmente com dados locais de demonstração.

VITE_API_URL=http://localhost:3333/api
